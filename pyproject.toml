[project]
name = "mod-doc"
version = "0.1.0"
description = "A modular documentation builder."
readme = "README.md"
requires-python = ">=3.12"

[project.scripts]
docgen = "docgen.main:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"